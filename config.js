// 当前启用的配置，目前没用，保留
export const profile = 'prod';

// 当前调用的基础域名地址
export const baseUrl = "http://localhost:8080"
export const loginUrl = "/api/v1/login"

// API 存储TOKEN的key
export const xxTokenKey = 'XX-Uni-Token'

// 返回的状态码
export const Code = {
	// 需要登录
	UNLOGIN: 401,
}

export const Constant = {
	
}

